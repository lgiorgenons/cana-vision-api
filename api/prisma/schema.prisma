// TODO: Modelar entidades conforme `Tasks/Planejamento_API.md` (seção 02).
// Exemplo de configuração inicial:
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
